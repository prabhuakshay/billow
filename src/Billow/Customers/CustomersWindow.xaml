<Window x:Class="Billow.Customers.CustomersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Billow.Customers"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:CustomersViewModel}"
        Title="Customers" Width="820" Height="520" MinWidth="560" MinHeight="320"
        ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        FocusManager.FocusedElement="{Binding ElementName=CustomersGrid}"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="Close" />
    </Window.InputBindings>

    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="Close_Executed" />
    </Window.CommandBindings>

    <DockPanel>
        <Border DockPanel.Dock="Bottom" Padding="12,10">
            <DockPanel LastChildFill="False">
                <Button DockPanel.Dock="Left" Content="_Add…" Width="80" Height="24"
                        Command="{Binding AddCommand}" />
                <Button DockPanel.Dock="Left" Content="_Edit…" Width="80" Height="24" Margin="8,0,0,0"
                        Command="{Binding EditCommand}" />
                <Button DockPanel.Dock="Right" Content="Close" Width="80" Height="24"
                        Command="Close" />
            </DockPanel>
        </Border>

        <!-- Double-clicking a row edits it. -->
        <DataGrid x:Name="CustomersGrid" ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False"
                  HeadersVisibility="Column" GridLinesVisibility="None" BorderThickness="0,0,0,1"
                  BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditCommand}" />
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="GSTIN" Binding="{Binding Gstin}" Width="130" />
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*" />
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*" />
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="130" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
